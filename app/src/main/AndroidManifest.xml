<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tzl.android_collection">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".custom_view.CustomViewActivity" />
        <activity android:name=".animator.AnimatorActivity" />
        <activity android:name=".database.DatabaseActivity" />
        <activity android:name=".photo_manage.PhotoManageActivity" />
        <activity android:name=".multi_media.MultiMediaActivity" />
        <activity android:name=".new_features.NewFeaturesActivity" />
        <activity android:name=".external_lib.ExternalLibActivity" />
        <activity android:name=".multi_media.ac.AudioPlayActivity" />
        <activity android:name=".external_sdk.ExternalSDKActivity" />
        <activity android:name=".major_knowledge_point.MajoyKnowPointActivity"></activity>
    </application>

</manifest>